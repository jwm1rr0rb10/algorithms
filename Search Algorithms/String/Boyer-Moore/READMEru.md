# Алгоритм Бойера-Мура (Boyer-Moore) для поиска подстроки

## Описание

**Бойер-Мур** — это один из самых быстрых алгоритмов поиска подстроки в строке. В отличие от большинства других алгоритмов, сравнение начинается с конца шаблона, что позволяет делать большие сдвиги и пропускать множество ненужных сравнений.

## Где применяется

- Поиск по большим текстам (редакторы, поисковые системы)
- Поиск сигнатур в антивирусах и системах фильтрации
- Биоинформатика (поиск последовательностей)
- Поиск в системах хранения и обработки данных

## Как работает

1. Предобрабатывает шаблон, строя таблицы «плохого символа» и «хорошего суффикса».
2. Сравнивает символы шаблона с текстом справа налево.
3. При несовпадении использует таблицы для сдвига шаблона на максимально возможное расстояние.
4. Повторяет процесс до конца текста.

## Сложность

- В среднем: значительно быстрее O(n), часто близко к O(n / m)
- В худшем случае: O(n + m)
- Память: O(m + k), где m — длина шаблона, k — размер алфавита

## Пример на Python

```python
def bad_character_table(pattern):
    table = {}
    for i, char in enumerate(pattern):
        table[char] = i
    return table

def boyer_moore_search(text, pattern):
    n, m = len(text), len(pattern)
    if m == 0:
        return []
    bad_char = bad_character_table(pattern)
    result = []
    shift = 0

    while shift <= n - m:
        j = m - 1
        while j >= 0 and pattern[j] == text[shift + j]:
            j -= 1
        if j < 0:
            result.append(shift)
            shift += m - bad_char.get(text[shift + m], -1) if shift + m < n else 1
        else:
            shift += max(1, j - bad_char.get(text[shift + j], -1))
    return result

# Пример использования:
text = "ABAAABCD"
pattern = "ABC"
print(boyer_moore_search(text, pattern))  # Выведет: [4]
```

## Преимущества

- Очень быстрый на практике для длинных шаблонов и больших текстов
- Позволяет делать большие сдвиги по тексту
- Применяется в стандартных библиотеках и промышленных решениях

---

Если нужны дополнительные примеры или сравнения с другими алгоритмами, дайте знать!